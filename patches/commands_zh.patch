--- a/hermes_cli/commands.py	2026-07-23 09:52:27.172037173 +0800
+++ b/hermes_cli/commands.py	2026-07-23 09:59:05.398062760 +0800
@@ -78,7 +78,7 @@
     CommandDef("save", "保存当前对话", "Session",
                cli_only=True),
     CommandDef("retry", "重试上一条消息（重新发送给代理）", "Session"),
-    CommandDef("prompt", "Compose your next prompt in $EDITOR (markdown), then send it", "Session",
+    CommandDef("prompt", "在 $EDITOR 中编写下一条提示（markdown），然后发送", "Session",
                cli_only=True, args_hint="[initial text]", aliases=("compose",)),
     CommandDef("undo", "回退 N 轮用户对话并重新提示（默认 1 轮）", "Session",
                args_hint="[N]"),
@@ -88,7 +88,7 @@
                args_hint="<platform>", cli_only=True),
     CommandDef("branch", "分支当前会话（探索不同方向）", "Session",
                aliases=("fork",), args_hint="[name]"),
-    CommandDef("compress", "Compress conversation context (add 'here [N]' to keep recent N turns; --preview shows what would happen)", "Session",
+    CommandDef("compress", "压缩对话上下文（加 'here [N]' 保留最近 N 轮；--preview 预览）", "Session",
                aliases=("compact",), args_hint="[here [N] | focus topic | --preview|--dry-run]"),
     CommandDef("rollback", "列出或恢复文件系统检查点", "Session",
                args_hint="[number]"),
@@ -97,13 +97,13 @@
     CommandDef("stop", "终止所有正在运行的后台进程", "Session"),
     CommandDef("approve", "批准待处理的危险命令", "Session",
                gateway_only=True, args_hint="[session|always]"),
-    CommandDef("deny", "Deny a pending dangerous command (optionally with a reason)", "Session",
+    CommandDef("deny", "拒绝待处理的危险命令（可附理由）", "Session",
                gateway_only=True, args_hint="[all] [reason]"),
     CommandDef("background", "在后台运行提示", "Session",
                aliases=("bg", "btw"), args_hint="<prompt>"),
     CommandDef("agents", "显示活动代理和正在运行的任务", "Session",
                aliases=("tasks",)),
-    CommandDef("journey", "Open the learning journey timeline",
+    CommandDef("journey", "打开学习旅程时间线",
                "Session", aliases=("learning", "memory-graph"), cli_only=True,
                args_hint="[list|delete <id>|edit <id>]",
                subcommands=("list", "delete", "edit")),
@@ -113,7 +113,7 @@
                args_hint="<prompt>"),
     CommandDef("goal", "设置持续目标，Hermes 跨轮持续工作直到达成", "Session",
                args_hint="[text | draft <text> | show | pause | resume | clear | status | wait <pid> | unwait]"),
-    CommandDef("moa", "Run one prompt through the default Mixture of Agents preset, then restore your model", "Session",
+    CommandDef("moa", "用默认 Mixture of Agents 预设运行一次提示，然后恢复原模型", "Session",
                args_hint="<prompt>"),
     CommandDef("subgoal", "添加或管理当前目标的附加条件", "Session",
                args_hint="[text | remove N | clear]"),
@@ -131,7 +131,7 @@
     # Configuration
     CommandDef("config", "显示当前配置", "Configuration",
                cli_only=True),
-    CommandDef("model", "Switch model (session-scoped; --global to persist)", "Configuration",
+    CommandDef("model", "切换模型（会话级；--global 全局持久化）", "Configuration",
                args_hint="[model] [--provider name] [--global|--session] [--refresh]"),
     CommandDef("codex-runtime", "切换 OpenAI/Codex 模型的 codex app-server 运行环境",
                "Configuration", aliases=("codex_runtime",),
@@ -141,10 +141,10 @@
                args_hint="[name]"),
     CommandDef("statusbar", "切换上下文/模型状态栏", "Configuration",
                cli_only=True, aliases=("sb",)),
-    CommandDef("timestamps", "Toggle [HH:MM] timestamps on messages and /history", "Configuration",
+    CommandDef("timestamps", "切换消息和 /history 上的 [HH:MM] 时间戳", "Configuration",
                cli_only=True, args_hint="[on|off|status]",
                subcommands=("on", "off", "status"), aliases=("ts",)),
-    CommandDef("verbose", "Cycle tool progress display: off -> new -> all -> verbose -> log",
+    CommandDef("verbose", "循环切换工具进度显示：关 -> 新增 -> 全部 -> 详细 -> 日志",
                "Configuration", cli_only=True,
                gateway_config_gate="display.tool_progress_command"),
     CommandDef("footer", "切换网关运行时元数据页脚（最终回复上）",
@@ -185,11 +185,11 @@
                subcommands=("pending", "approve", "reject", "approval")),
     CommandDef("bundles", "列出技能捆绑包（别名 /<名称> 对应多个技能）",
                "Tools & Skills"),
-    CommandDef("pet", "Toggle or adopt a petdex mascot (/pet, /pet list, /pet <slug>)", "Tools & Skills",
+    CommandDef("pet", "切换或领养 petdex 吉祥物（/pet, /pet list, /pet <slug>）", "Tools & Skills",
                cli_only=True, args_hint="[toggle|list|scale <n>|<slug>]", subcommands=("toggle", "list", "scale", "off")),
-    CommandDef("hatch", "Generate a new petdex pet from a description",
+    CommandDef("hatch", "从描述生成一个新的 petdex 宠物",
                "Tools & Skills", cli_only=True, aliases=("generate-pet",), args_hint="[description]"),
-    CommandDef("learn", "Learn a reusable skill from anything you describe (dirs, URLs, this chat, notes)",
+    CommandDef("learn", "从你描述的任何内容学习可复用技能（目录、URL、本对话、笔记）",
                "Tools & Skills", args_hint="<what to learn from>"),
     CommandDef("cron", "管理定时任务", "Tools & Skills",
                cli_only=True, args_hint="[subcommand]",
@@ -228,11 +228,11 @@
     CommandDef("help", "显示可用命令", "Info"),
     CommandDef("restart", "在排空活动运行后优雅重启网关", "Session",
                gateway_only=True),
-    CommandDef("usage", "Show token usage and rate limits; `reset` redeems a banked Codex limit reset", "Info",
+    CommandDef("usage", "显示令牌用量和速率限制；`reset` 兑换已存储的 Codex 限制重置", "Info",
                args_hint="[reset [--force]]"),
-    CommandDef("subscription", "View your Nous plan and change it in the browser", "Info",
+    CommandDef("subscription", "查看你的 Nous 套餐并在浏览器中更改", "Info",
                cli_only=True, aliases=("upgrade",)),
-    CommandDef("topup", "Show your Nous balance and manage billing on the portal", "Info"),
+    CommandDef("topup", "查看你的 Nous 余额并在门户管理账单", "Info"),
     CommandDef("insights", "显示使用统计和分析", "Info",
                args_hint="[days]"),
     CommandDef("platforms", "显示网关/消息平台状态", "Info",
